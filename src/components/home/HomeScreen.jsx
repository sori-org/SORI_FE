import styled from 'styled-components';

const HomeScreen = () => {
    return (
        <Container>
            <div>홈 페이지!</div>
        </Container>
    );
};

export default HomeScreen;

const Container = styled.div`
    overflow-y: auto;
    align-items: center;
`;
